id: prefer-symbol-over-proc
valid:
  - |
      list.map(&:to_s)
  - |
      list.select(&:even?)
  - |
      items.each { |v| puts v.name; v.save }
invalid:
  - |
      list.map { |v| v.to_s }
  - |
      list.select { |v| v.even? }
  - |
      list.each { |v| v.upcase }
