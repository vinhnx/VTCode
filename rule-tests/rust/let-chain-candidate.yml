id: let-chain-candidate
valid:
  - |
      if let Some(x) = a {
          println!("{}", x);
      } else {
          println!("none");
      }
  - |
      if a > 0 {
          if b > 0 {
              if c > 0 {
                  println!("all positive");
              }
          }
      }
  - |
      if let Some(x) = a {
          println!("{}", x);
          extra_call();
          if let Some(y) = b {
              println!("{}", y);
          }
      }
invalid:
  - |
      if let Some(x) = a {
          if let Some(y) = b {
              println!("{} {}", x, y);
          }
      }
  - |
      if a > 0 {
          if let Some(x) = b {
              println!("{}", x);
          }
      }
