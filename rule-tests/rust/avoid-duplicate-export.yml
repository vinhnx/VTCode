id: avoid-duplicate-export
valid:
  - |
      pub mod foo;
      pub use foo::Bar;
  - |
      pub mod utils;
      pub use utils::Helper;
  - |
      pub mod foo;
      // No pub use of foo's items
  - |
      mod foo;
      pub use foo::Bar;
  - |
      pub mod foo;
      pub use bar::Baz;
