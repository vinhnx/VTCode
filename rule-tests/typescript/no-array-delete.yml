id: no-array-delete
valid:
  - |
      arr.splice(index, 1);
  - |
      const filtered = arr.filter((_, i) => i !== index);
  - |
      delete obj.prop;
invalid:
  - |
      delete arr[0];
  - |
      delete items[i];
