id: no-unnecessary-let
valid:
  - |
      obj?.let { println(it) }
  - |
      obj.let { value -> process(value) }
  - |
      name.let { n -> n.length }
invalid:
  - |
      obj.let { it.toString() }
  - |
      name.let { it.length }
