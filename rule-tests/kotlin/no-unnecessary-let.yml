id: no-unnecessary-let
valid:
  - |
      obj?.let { println(it) }
  - |
      obj.let { it.toString() }
  - |
      name.let { it.length }
invalid:
  - |
      result.let { value -> process(value) }
  - |
      name.let { n -> n.length }
