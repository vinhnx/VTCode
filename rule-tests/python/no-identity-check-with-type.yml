id: no-identity-check-with-type
valid:
  - |
      isinstance(x, int)
  - |
      isinstance(x, (int, float))
  - |
      if x is None:
          pass
  - |
      type(x)
invalid:
  - |
      type(x) is int
  - |
      type(x) == str
