id: no-manual-dict-merge
valid:
  - code: |
      merged = {**a, **b}
  - code: |
      merged = a | b
  - code: |
      for k, v in items.items():
          if v is not None:
              result[k] = v
  - code: |
      for item in items:
          process(item)
invalid:
  - code: |
      for k, v in other.items():
          target[k] = v
      return target
