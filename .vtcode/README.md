# VT Code Workspace Files

- Put always-on repository guidance in `AGENTS.md`.
- Put path-scoped prompt rules in `.vtcode/rules/*.md` with YAML frontmatter.
- Keep authoring notes and other workspace docs outside `.vtcode/rules/` so they stay out of prompt memory.
