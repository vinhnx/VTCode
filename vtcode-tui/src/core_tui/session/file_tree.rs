//! Legacy file tree module intentionally left empty after removing the
//! tui-tree-widget dependency. Retained only so older build artifacts that
//! reference the module path continue to compile cleanly.
