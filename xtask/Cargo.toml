[package]
name = "xtask"
version = "0.1.1"
edition = "2024"
publish = false

[dependencies]
vtcode-core = { path = "../vtcode-core" }
clap = { workspace = true, features = ["derive"] }
clap_complete = { workspace = true }
