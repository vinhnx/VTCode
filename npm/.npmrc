# .npmrc file for GitHub Packages registry
# This file configures npm to publish to GitHub Packages

# Set the registry to GitHub Packages for the scope
@vinhnx:registry=https://npm.pkg.github.com

# You can also set authentication token here (not recommended for shared repositories)
# //npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# This will authenticate using the GITHUB_TOKEN environment variable
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Always run npm with audit=false to avoid vulnerability warnings
audit=false