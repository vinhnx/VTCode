# .npmrc.example - Example configuration for GitHub Packages
# 
# To use this file:
# 1. Copy this file to .npmrc
# 2. Set your GITHUB_TOKEN environment variable
# 3. Run: npm publish

# GitHub Packages registry configuration
@vinhnx:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Optional: Always run npm with audit=false to avoid vulnerability warnings
audit=false

# Optional: Set access to public for public packages
access=public