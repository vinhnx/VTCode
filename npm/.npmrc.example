# NPM Registry Configuration for VT Code
#
# This file configures npm to publish the vtcode package to both npmjs.org and GitHub Packages
#
# Setup Instructions:
# 1. For npmjs.org: Create an npm access token with publish scope
#    See: https://docs.npmjs.com/creating-and-viewing-access-tokens
#
# 2. For GitHub Packages: Create a GitHub Personal Access Token (PAT) with scopes:
#    - write:packages
#    - read:packages
#    - repo
#    See: https://github.com/settings/tokens
#
# 3. Configure your local NPM registries:
#    Either use the .npmrc file or run these commands:
#
#    # For npmjs.org
#    npm config set //registry.npmjs.org/:_authToken YOUR_NPM_TOKEN
#
#    # For GitHub Packages
#    npm config set //npm.pkg.github.com/:_authToken YOUR_GITHUB_TOKEN
#
#    # Or use environment variables during publish
#    export NODE_AUTH_TOKEN=YOUR_NPM_TOKEN  # for npmjs.org
#    export NODE_AUTH_TOKEN=YOUR_GITHUB_TOKEN  # for GitHub Packages
#
# 4. To use this .npmrc file, copy it to your home directory:
#    cp npm/.npmrc.example ~/.npmrc
#    Then add your tokens to ~/.npmrc or use the commands above
#
# Configuration for npmjs.org (default registry)
//registry.npmjs.org/:_authToken=YOUR_NPM_TOKEN
# Configuration for GitHub Packages registry
//npm.pkg.github.com/:_authToken=YOUR_GITHUB_TOKEN

# If using token-based authentication via environment variable,
# the .npmrc.example file can stay as-is with placeholder tokens.
# If using hardcoded tokens (NOT RECOMMENDED for public repos),
# replace the placeholder tokens with your actual tokens:
# //registry.npmjs.org/:_authToken=ACTUAL_NPM_TOKEN_HERE
# //npm.pkg.github.com/:_authToken=ACTUAL_GITHUB_TOKEN_HERE
